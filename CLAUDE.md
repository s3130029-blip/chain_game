# CLAUDE.md

> このファイルは **Claude Code 向けの運用ルール**であり、人間向けの紹介文ではない。
> 実装を始める前に必ず本ファイルを読み、以降のすべての作業で従うこと。
> 迷ったら「推測で進めず、止まって確認する」が大原則。
> **返答・報告・質問はすべて日本語で行うこと。**（コードとコメントは英語のまま）

---

## 1. Project Vision

このゲームの本質は **「移動による連鎖コンボを設計し、その結果を観戦すること」**。

- 着想：**キングオブワンズ**の移動連鎖の面白さ ＋ **激突要塞！**の共有コード文化。
- プレイヤーは **ユニットを配置するだけ**。戦闘は **自動・決定論的に進行**する。勝負は操作技術ではなく **構築力と連鎖設計**で決まる。
- 非同期対戦：盤面を **共有コード（文字列）** にして渡し、相手の盤面とぶつける。**サーバー不要**。
- 重視**しない**もの：リアルタイム操作、アクション性、反射神経、収集要素・レア度。
- 現フェーズ：**UIなしの戦闘エンジン**を完成させる（描画は次フェーズ）。

**なぜ重要か**：実装判断はすべて「連鎖が設計して面白いか／観て分かるか」と「サーバーレスかつ非同期で破綻しないか」に資するべき。この軸から外れる実装（操作性の追加、リアルタイム化、サーバー依存）は本質に反する。

---

## 2. Read Order

タスク着手前に、必ず次の順で読む。

1. **CLAUDE.md**（本ファイル）— 全作業を縛る運用ルール
2. **docs/architecture.md** — 設計意図・クラス/モジュール構成・依存DAG・決定論ルール
3. **docs/tasks.md** — これから着手する **該当タスク1件**とその完了条件
4. **docs/test-spec.md** — その該当タスクに対応する **Given/When/Then のテストケース**

> タスク単位の作業では「tasks.md の対象タスク」と「test-spec.md の対応ケース」をピンポイントで読むこと。
> **なぜ重要か**：設計判断はすでに3ファイルで確定済み。読まずに着手すると、決定済みの仕様を再発明・逸脱し、一貫性が崩れる。

Reference Documents

- design/要件定義書_連鎖要塞.md

企画意図の確認が必要な場合のみ参照すること

---

## 3. Development Principles

- **テスト駆動（TDD）**：実装より先に、test-spec の該当ケースを Vitest テストへ変換する。テストが緑になるまで実装し続ける。
  *なぜ*：仕様を実行可能な形で固定し、実装のブレと回帰を防ぐ。
- **小さなタスク単位**：1セッション＝原則 **tasks.md の1タスク**。複数を勝手にまとめない。
  *なぜ*：レビュー可能性と切り戻し容易性。大きな変更は破綻時の被害が大きい。
- **MVP優先・エンジンファースト**：現フェーズは戦闘エンジンのみ。UI/描画/演出を作らない。
  *なぜ*：最大リスク（連鎖が面白いか）をUI着手前に検証するため（tasks.md T7.2 が Go/No-Go）。
- **決定論を絶対に崩さない**：§5 を最優先で順守。
  *なぜ*：非同期対戦の公平性とリプレイ再現の根幹。崩れると製品全体が静かに壊れる。
- **データ駆動のユニット定義**：ユニットは「action ＋ triggers」のデータで表現し、エンジンが解釈する。
  *なぜ*：新ユニット追加を定義データの追加で済ませ、エンジンを不変に保つ。
- **依存は一方向（DAG）**：`core → model → (units/effects/events) → engine → simulator`。逆流・循環を作らない。
  *なぜ*：テスト容易性と変更影響の局所化。

---

## 4. Source of Truth（仕様競合時の優先順位）

仕様が食い違ったら、次の優先順位で判断する（上が強い）。

1. **CLAUDE.md** — プロセスと決定論の非交渉ルール。これに反する実装はテストが要求しても採用しない。
2. **test-spec.md** — **観測可能な振る舞い**（入力に対する期待結果）の最終真実。具体的かつ実行可能。
3. **architecture.md** — 構成・シグネチャ・依存規則・決定論ルールの詳細。
4. **tasks.md** — 作業範囲と順序のみを定める。

**競合時の手順（必須）**：
- 競合を見つけたら **推測で片方に寄せて実装しない**。
- 競合内容・該当箇所・選択肢を明示し、**真実とすべきファイルを先に修正する提案**を出してから進む。
- 例：architecture.md と test-spec.md で挙動が食い違う場合、振る舞いは test-spec が優先だが、**architecture.md を黙って無視せず、ズレの修正を提案する**。

> **なぜ重要か**：黙って解決すると、ドキュメント間の不整合が残り、次の Claude Code がまた迷う。真実を一箇所に収束させ続けることが一貫性の条件。

---

## 5. Determinism Rules（最重要・非交渉）

エンジン（`core/model/units/effects/events/engine/codec`）では以下を厳守。

- **乱数禁止**：`Math.random()` を使わない。乱数が要る場合のみ `core/rng.ts` のシード付き擬似乱数を使い、**シードは入力（共有コード等）に含める**。
- **時刻・環境依存禁止**：`Date.now()` / `performance.now()` / ロケール / タイムゾーンに依存しない。
- **反復順への依存禁止**：`Map` / `Set` / オブジェクトキーの列挙順をゲームロジックの判断に使わない。順序が要る所は**必ず明示ソート**する。
  - 行動順：`speed` 降順 → 読み順（`y` 昇順 → `x` 昇順）。
  - 連鎖：FIFO キュー。同時に生じた反応は enqueue 前に読み順で安定ソート。
- **押し出し**：前方から再帰解決。盤外に出たユニットは撃破（恒久的な詰まりは存在しない）。
- **死亡処理**：HP0 はステップ境界でまとめて除去し、`onDeath` を発火。
- **無限ループ防止**：1手番の連鎖は `MAX_CASCADE_STEPS` で打ち切る。
- **非同期/IO禁止**：エンジン内で `async`/`await`/Promise/`fetch`/ネットワーク/ブラウザAPIを使わない。
- **整数で扱う**：座標・HP は整数。浮動小数の比較・累積に依存しない（プラットフォーム差を避ける）。
- **不変条件**：**同一の2盤面から `runBattle` は常に完全一致の `events` を返す**（test-spec **SUP-07** が常に緑）。

> **なぜ重要か**：共有コード2つだけから誰の環境でも同じ結果・同じリプレイが再現されることが、このゲームの非同期対戦そのもの。決定論が1箇所でも破れたら、対戦結果が環境依存になり製品が成立しない。

---

## 6. Coding Guidelines（TypeScript）

- **strict 前提**：`strict: true` / `noUncheckedIndexedAccess: true` / `exactOptionalPropertyTypes: true`。型エラーを残さない。
- **`any` 禁止**。判別共用体（discriminated union）を使い、`switch` は網羅。漏れは `never` で検出する（`const _exhaustive: never = x`）。
- **エンジンは純粋関数中心**：グローバル可変状態・シングルトンを作らない。状態は `BattleState` を引数で明示的に渡す。
- **依存方向を守る**：エンジンは DOM/ブラウザAPIを import しない。UI がエンジンに依存する（逆は禁止）。architecture.md の依存DAGにない新規クロス依存を作らない。
- **公開シグネチャは architecture.md に従う**：`resolvePush` / `collectReactions` / `resolveCascade` / `runBattle` / `encodeBoard` / `decodeBoard` 等の名前・引数・戻り値を勝手に変えない。変更が必要なら **先に architecture.md を直す**。
- **1モジュール1責務**。ファイルの肥大化を避け、tasks.md の分割に沿う。
- **イベントログの出力は読み取り専用**（`readonly`）で返す。
- **依存追加は最小限**：MVP の実行時依存は `lz-string` のみ（開発時に `vite`/`vitest`/`tsx`）。新規依存は無断追加しない（§10）。
- **エラーは握り潰さない**：`decoder` は不正入力を検証して例外を投げる。例外を黙って飲み込まない。
- **コメントは「なぜ」を書く**。「何を」はコードで表す。冗長なコメントを足さない。

> **なぜ重要か**：型と純粋性と一方向依存が、決定論・テスト容易性・UI差し替え可能性を同時に支えている。

---

## 7. Testing Policy（Vitest）

- **テスト先行**：実装前に test-spec の該当ケースを Vitest（`describe`/`it`）へ変換する。
- **トレーサビリティ**：テスト名にケースIDを入れる（例：`it('PR-07: 連鎖押しの先頭が盤外撃破', ...)`）。
- **co-located**：`*.test.ts` を対象ソースの隣に置く。
- **フィクスチャはヘルパー経由**：`tests/helpers.ts`（`makeState` / `posOf` / `isAlive` / `kindsOf`）を使い、盤面構築を統一する。
- **安定フィールドのみ assert**：test-spec §0 に挙げた形のみ検証し、未規定フィールド（攻撃元attribution等）はアサートしない。
- **決定論テスト SUP-07 は常に緑**。固定盤面のゴールデンマスター（`toMatchSnapshot`）で回帰を検出する。
- **テストを甘くして緑にしない**：テストが間違っていると判断したら、**勝手に書き換えず**、test-spec の修正を提案してから直す。
- **完了の定義**：`npm test`（全緑）かつ `npm run typecheck`（エラーなし）。両方通るまでタスク完了としない。
- **`.only` / `.skip` / 失敗テストをコミットしない**。

> **なぜ重要か**：test-spec が実行可能な仕様。これを緩めたり迂回したりすると、仕様そのものが空洞化する。

---

## 8. Task Execution Policy

- **対象は1タスクのみ**：指示がない限り tasks.md の1タスクだけ実装する。
- **スコープ外のファイルに触れない**：そのタスクの「作成/編集」に列挙されたファイル＋対応テスト＋`tests/helpers.ts` のみ編集する。
- **勝手に機能追加しない**：タスク/仕様にないユニット・パラメータ・オプション・最適化・UIを足さない。
- **勝手に仕様変更しない**：実装の都合で仕様や architecture を曲げない。仕様が誤りと判断したら **止まって提案**する。
- **前提タスクを尊重**：依存（prerequisite）が未完のタスクを先に実装しない。
- **バランス値は据え置き**：ユニットの speed/HP 等は tasks.md T7.2 まで仮置き。早期に「改善」しない。
- **曖昧なら止まる**：判断に迷う点は、推測で進めず、論点と選択肢を提示してから進む。
- **完了したら tasks.md のチェックを更新**する。

> **なぜ重要か**：段階的開発は「各タスクが小さく・自己完結・検証可能」であることで機能する。スコープ越境や勝手な追加は、後続タスクの前提を壊し一貫性を失わせる。

---

## 9. Git Workflow

- **1タスク＝1コミット**（小さな関連変更のまとまりは可）。
- **コミットメッセージにタスクIDを含める**：例 `T4.2: implement PushResolver (PR-01..PR-11)` / `T5.4: cascade resolver with loop guard`。
- 形式の目安：`<type>(<scope>): <TaskID> <summary>`（例 `feat(engine): T5.6 runBattle simulator`）。
- **緑のものだけコミット**：失敗テスト・`.only`・コメントアウトした死んだコード・`dist/`・`node_modules/` をコミットしない。
- **変更を混ぜない**：無関係な変更を同一コミットに含めない。
- **push 済み履歴を書き換えない**（force-push でのリベース等をしない）。`main` は常にテストが通る状態を保つ。

> **なぜ重要か**：タスクIDに紐づく粒度の揃ったコミットが、レビュー・原因特定・切り戻しを容易にする。

---

## 10. Forbidden Actions（絶対禁止）

- ❌ **エンジンに非決定性を持ち込む**：`Math.random()` / `Date.now()` / 反復順依存 / `async`・Promise / ネットワーク（§5違反）。
- ❌ **現フェーズで UI・DOM・描画・演出を実装する**（エンジンファースト違反）。
- ❌ **公開シグネチャを architecture.md 更新なしに変更する**。
- ❌ **失敗テストを `.skip`/削除/緩和して緑に見せる**。
- ❌ **指定タスクのスコープ外ファイルを編集する**。
- ❌ **無断で実行時依存を追加する**（MVP は `lz-string` のみ）。
- ❌ **サーバー/外部API/Firebase/ログイン/サーバー保存を MVP に入れる**（サーバーレス・ログイン不要の前提を破る）。
- ❌ **`localStorage` 等のブラウザ専用APIをエンジンで使う**。
- ❌ **tasks.md / test-spec.md にない新ユニット・新メカニクス・新機能を発明する**。
- ❌ **仕様競合を推測で黙って解決する**（必ず §4 の手順で surface する）。

> **なぜ重要か**：上記はいずれも「決定論」「サーバーレス非同期」「段階的開発の一貫性」という本プロジェクトの土台を直接壊す。ひとつでも破ると、後から検出しづらい形で製品が破綻する。

---

## When in Doubt（最後の指針）

判断に迷ったら、次の順で考える。
1. **§5 決定論を破らないか？** 破るなら、その実装は誤り。
2. **現タスクのスコープ内か？**（§8）外なら触らない。
3. **test-spec / architecture と一致するか？**（§4）食い違うなら止めて提案。
4. それでも不明なら、**推測で実装せず、論点を明示して確認を求める**。
